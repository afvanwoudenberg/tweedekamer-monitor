-- Run this file once to initialize the environment

-- Setup schema to store tables in
CREATE SCHEMA IF NOT EXISTS workspace.tweedekamer;

-- Set default catalog and schema for subsequent queries
USE CATALOG workspace;
USE SCHEMA tweedekamer;
